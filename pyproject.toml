[project]
name = "my-test-project"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "torch==2.2.2",
    "torchvision==0.17.2",
    "numpy<2",
]
[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
